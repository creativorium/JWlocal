# Dependencies & build output (never committed, never deployed from repo)
node_modules/
dist/
*.local

# Vite dev marker — must NEVER reach EasyWP
dist/hot

# Archives / scratch / site backups (never source)
*.zip
*.tar.gz
JWtradingLocal/

# OS noise
Thumbs.db
.DS_Store

# Local dev / planning / source assets — kept on disk, NOT in the repo.
# (The repo should carry only the deploy workflow, theme, and plugins.)
.claude/
CLAUDE.md
jwtrading/CLAUDE.md
tools/
blogplan.md
JOURNAL-INTEGRATION.md
PROJECT-NOTES.md
PANDUAN-TRANSFER-MANUAL.md

# Journal webhook — kept local-only until the token is ready (not on production).
jwtrading/wp-content/plugins/jwtrading-core/includes/class-journal-sync.php
headerfooter.html
backupthemefunction and style.txt
Brand Partner Logos/
Home Page Icons/
